# US RS+T Stock Screener

美股強勢股篩選器 — 結合 **RS Rank（雙週期相對強度）** 與 **T-Value（趨勢品質）** 框架。
透過 Telegram Bot 每日自動篩選，支援回測與個股診斷。

## 篩選流程

```
全市場
  → 排除 ETF / ADR / 衍生品
  → 股價 > $10.01、日均成交額 > $800萬
  → RS(20) 百分位 ≥ 85%
  → RS(60) 百分位 ≥ 70%
  → RS(20) > RS(60)（動量加速）
  → T60 前 30%、T120 ≥ 中位數
  → 量增、波動率過濾、距 52 週高點 ≥ 85%
  → 綜合評分 → Top 5
```

## Bot 指令

| 指令 | 功能 | 範例 |
|------|------|------|
| `/now` | 立即執行篩選 | `/now` |
| `/YYMMDD` | 回測指定日期 + 3 個月前瞻表現 | `/250101` |
| `/YYMMDD SYMBOL` | 診斷個股是否入選（逐項檢查） | `/250101 AAPL` |
| `/start` | 顯示說明 | `/start` |
| `/status` | 系統狀態 | `/status` |

### 回測輸出範例

```
📊 US RS+T Screener — 2025-01-01

#  Ticker          RS20  RS60    T60  Score
────────────────────────────────────────────
1  NASDAQ:NVDA       95    82  0.483  0.891
2  NYSE:PLTR         92    78  0.412  0.854

📈 3個月前瞻表現
#  Ticker          最高%    MDD%  天數
──────────────────────────────────────
1  NASDAQ:NVDA     +32.1   -8.3    63
2  NYSE:PLTR       +28.4   -6.7    63
```

### 個股診斷範例

```
🔍 AAPL 檢查清單 — 2025-01-01

✅ 股價 $182.30 ≥ $10.01
✅ 20日均成交額 $12345.0M ≥ $8M
✅ RS(20) 百分位 88.3 ≥ 85
✅ RS(60) 百分位 75.2 ≥ 70
✅ 動量加速 RS20(88.3) > RS60(75.2)
✅ T60 = 0.4120 (≥ 門檻 0.2831)
❌ 量增 20d/50d = 0.92 < 1.0
❌ 最終結果: 未入選
```

## 部署

### 1. Telegram Bot

1. @BotFather → `/newbot` → 取得 Token
2. 啟動 bot → `/start` → 取得 Chat ID

### 2. GitHub → Zeabur

```bash
git init && git add -A
git commit -m "init"
git remote add origin https://github.com/YOUR/us-rs-t-screener.git
git push -u origin main
```

Zeabur：New Project → Deploy from GitHub → 設定環境變數：

- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_CHAT_ID`

### 3. 本地測試

```bash
pip install -r requirements.txt
cp .env.example .env   # 填入 token & chat ID

python screener.py     # 只跑篩選（不啟動 bot）
python bot.py          # 啟動 bot
```

## TXT 輸出

檔名：`US-RS+T_YYMMDD.txt`

```
NASDAQ:NVDA
NYSE:PLTR
NASDAQ:META
```

- 自動加交易所前綴 (`NASDAQ:` / `NYSE:` / `AMEX:`)
- Class share：`BRK-B` → `BRK.B`

## 技術說明

### RS Rank（雙週期）

```
RS(20) = close[-1] / close[-21] - 1    → 全市場百分位排序
RS(60) = close[-1] / close[-61] - 1    → 全市場百分位排序
```

篩選條件：RS(20) pct ≥ 85 **且** RS(60) pct ≥ 70 **且** RS(20) pct > RS(60) pct

### T-Value（趨勢品質）

1. **標準化**：10 日 MA 將 K 線轉為位移序列 S
2. **T1**（連續波段法）：拐點位移的平方和
3. **T2**（絕對波動區間法）：遞迴分解最大擺幅
4. **T = max(T1, T2) / N^1.5**
5. **T_w**：ATR 加權版，保留漲跌幅度
6. **最終 T = 0.5 × T_std + 0.5 × T_w**

### 參數調整

所有參數在 `config.py`：

| 參數 | 預設 | 說明 |
|------|------|------|
| `TOP_N` | 5 | 輸出股票數 |
| `RS20_PERCENTILE_CUTOFF` | 85 | RS(20) 門檻 |
| `RS60_PERCENTILE_CUTOFF` | 70 | RS(60) 門檻 |
| `T60_PERCENTILE_CUTOFF` | 70 | T60 門檻（前30%）|
| `T_MA_PERIOD` | 10 | 標準化均線週期 |
| `NEAR_52W_HIGH_PCT` | 0.85 | 距52週高點比例 |

## 注意事項

- 全市場下載約 10-20 分鐘，回測亦同
- 回測使用當前股票池（存在倖存者偏差）
- yfinance 免費數據可能偶爾限速，如遇不穩可將 `YFINANCE_BATCH_SIZE` 降至 200
- `python-telegram-bot>=20` 為唯一有版本要求的套件（v13→v20 為 breaking change）
